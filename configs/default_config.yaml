system:
  seed: 42
  log_level: "INFO"

model:
  name: "Qwen/Qwen2-VL-7B-Instruct"
  quantization: "4bit"
  device_map: "auto"
  max_new_tokens: 512

retrieval:
  db_path: "./data/chroma_db"
  embedding_model: "BAAI/bge-small-zh-v1.5"
  collection_name: "medical_kb_v1"
  top_k: 3

safety:
  enable_red_flag_detection: true

evaluation:
  results_dir: "./results"
